# TotemAPI - Variáveis de Ambiente
# Copie este arquivo para .env e preencha os valores REAIS

# === DATABASE (obrigatório) ===
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/totemdb
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD=your_password_here

# === SECURITY (obrigatório) ===
# Gerar chave: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_256bits_here

# === CORS (obrigatório para deploy) ===
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://seu-frontend.vercel.app

# === LGPD - Criptografia (obrigatório) ===
# Gerar chave: openssl rand -base64 32
TOTEM_ENCRYPTION_KEY=your_encryption_key_256bits_here

# === EMAIL (opcional) ===
RESEND_API_KEY=re_your_resend_key_here
RESEND_FROM_EMAIL=noreply@seudominio.com
RESEND_WEBHOOK_SECRET=your_resend_webhook_secret

# === WEBHOOK GHL (opcional) ===
GHL_WEBHOOK_SECRET=your_ghl_secret_here

# === PIX/PAYMENT (opcional - não necessário para modo simulado) ===
# ASAAS_API_URL=https://sandbox.asaas.com/api/v3
# ASAAS_API_KEY=your_asaas_api_key_here
