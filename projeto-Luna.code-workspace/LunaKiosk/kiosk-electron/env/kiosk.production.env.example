# LunaKiosk (Electron) - PRODUÇÃO
# Copie este arquivo para um destes locais com o nome exatamente ".env":
# 1) Portable: na mesma pasta do .exe
# 2) Instalado: %LOCALAPPDATA%\Programs\LunaKiosk\.env
# 3) Alternativo: %LOCALAPPDATA%\LunaKiosk\userData\.env
# (não commite segredos)

# --- Ambiente Asaas ---
ASAAS_ENVIRONMENT=production
ASAAS_PROD_API_KEY=
ASAAS_PROD_WALLET_ID=

# Recomendado
ASAAS_WEBHOOK_SECRET=

# --- Backends (opcional) ---
# Se quiser que o Electron NÃO suba os JARs (porque você já está rodando via Docker/terminal):
# KIOSK_SKIP_BACKENDS=true

# Em alguns Windows, "localhost" resolve para IPv6 (::1) e pode dar ERR_CONNECTION_REFUSED.
# Use 127.0.0.1 para forçar IPv4:
KIOSK_HOST=127.0.0.1

# Se sua máquina tiver problemas de GPU/cache:
# KIOSK_DISABLE_GPU=true

# Se precisar testar sem exigir Asaas (não recomendado):
# KIOSK_REQUIRE_ASAAS=false
