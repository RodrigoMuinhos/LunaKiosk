# Exemplo de .env para o LunaKiosk (Electron)
# Coloque este arquivo como .env ao lado do executável (instalado/portable)
# OU em %LOCALAPPDATA%\LunaKiosk\userData\.env
# (NUNCA commitar segredos)

# --- Controle do Electron ---
# Desligue apenas para testes (não recomendado):
# KIOSK_REQUIRE_ASAAS=false

# Porta do TotemUI embarcado (opcional)
# KIOSK_PORT=19007
# KIOSK_HOST=localhost

# --- Asaas (obrigatório em produção) ---
# Escolha: sandbox | production
ASAAS_ENVIRONMENT=sandbox

# Sandbox
ASAAS_SANDBOX_API_KEY=coloque_sua_chave_aqui
ASAAS_SANDBOX_WALLET_ID=coloque_sua_wallet_id_aqui

# Produção (se ASAAS_ENVIRONMENT=production)
# ASAAS_PROD_API_KEY=coloque_sua_chave_aqui
# ASAAS_PROD_WALLET_ID=coloque_sua_wallet_id_aqui

# Webhook (recomendado)
# ASAAS_WEBHOOK_SECRET=coloque_seu_webhook_secret_aqui
