# =========================================================
# EXEMPLO (sem segredos) — copie para .env e preencha
# =========================================================

# Security
JWT_SECRET=CHANGE_ME_USE_A_LONG_RANDOM_SECRET

# Banco (Neon/PostgreSQL) — ajuste schema/credenciais
# Formato: jdbc:postgresql://HOST/DB?user=...&password=...&sslmode=require&currentSchema=...
NEON_LUNACORE_URL=jdbc:postgresql://HOST/DB?user=USER&password=PASS&sslmode=require&currentSchema=luna
NEON_TOTEMAPI_URL=jdbc:postgresql://HOST/DB?user=USER&password=PASS&sslmode=require&currentSchema=luna
NEON_LUNAPAY_URL=jdbc:postgresql://HOST/DB?user=USER&password=PASS&sslmode=require&currentSchema=luna

# URLs internas (Docker usa hostnames; Electron/local usa localhost)
LUNACORE_URL=http://127.0.0.1:8080
TOTEM_API_BASE_URL=http://127.0.0.1:8081

# Asaas (LunaPay)
ASAAS_ENVIRONMENT=sandbox
ASAAS_SANDBOX_API_KEY=
ASAAS_SANDBOX_WALLET_ID=
ASAAS_PROD_API_KEY=
ASAAS_PROD_WALLET_ID=
ASAAS_WEBHOOK_SECRET=

# Electron (opcionais)
SPRING_PROFILES_ACTIVE=production
KIOSK_PORT=19007
KIOSK_START_URL=/

# Workarounds para ambiente Electron/kiosk
# Em builds empacotados, o app pode desabilitar webSecurity (CORS) para evitar "Failed to fetch" no login.
# Use true para reativar (pode exigir ajuste de CORS no backend):
# KIOSK_ENABLE_WEB_SECURITY=true

# Desliga o workaround de headers CORS no Electron (normalmente não precisa):
# KIOSK_DISABLE_CORS_WORKAROUND=true

# Limpar sessão (localStorage/cookies) ao abrir o app.
# Recomendado para kiosk para não abrir CRM automaticamente por sessão antiga.
KIOSK_CLEAR_SESSION_ON_START=true

# Se sua máquina der erro de cache/GPU ("Acesso negado"), habilite:
KIOSK_DISABLE_GPU=true

# Se precisar mudar onde o Electron grava userData/cache:
# KIOSK_USERDATA_DIR=C:\\Temp\\LunaKiosk

# Para não subir os JARs (se você já está rodando backend fora):
# KIOSK_SKIP_BACKENDS=true
